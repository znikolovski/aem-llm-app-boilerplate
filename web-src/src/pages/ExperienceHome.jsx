export function ExperienceHome() {
  return (
    <div>
      <h2>Live experience</h2>
      <p className="muted">
        Streaming tokens stay in the chat panel. When the model calls the <code>recommend</code> tool, this area
        navigates early with skeleton cards, then fills in structured UI from the App Builder action.
      </p>
    </div>
  );
}
