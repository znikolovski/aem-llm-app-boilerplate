{
  "name": "adobe-app-builder-llm-app-boilerplate",
  "version": "0.1.0",
  "private": true,
  "description": "Adobe App Builder boilerplate: streaming Responses API chat, tool actions, and a React SPA with a shared UI-block contract for multi-brand LLM apps.",
  "license": "Apache-2.0",
  "workspaces": [
    "web-src"
  ],
  "scripts": {
    "build": "tsc --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test .cache/test/test/**/*.test.js",
    "app:dev": "aio app dev",
    "app:build": "aio app build",
    "app:deploy": "aio app deploy"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "ts-loader": "^9.5.2",
    "typescript": "^5.8.3",
    "webpack": "^5.98.0"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "openai": "^5.23.2"
  }
}
